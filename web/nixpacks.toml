# Nixpacks configuration for Railway
# This tells Railway how to build the React/Vite frontend

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

# Serve the static files
[start]
cmd = "npx serve dist -s -l ${PORT:-3000}"
